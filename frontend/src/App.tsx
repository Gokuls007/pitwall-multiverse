/**
 * Phase 0 placeholder shell. The real UI (RaceSelector, GapChart,
 * StrategyTimeline, DecisionPanel, Classification, MultiverseTree) is built in
 * Phases 6–7 against a written design plan (spec Part 11.1).
 */
export default function App() {
  return (
    <main className="min-h-screen bg-neutral-950 text-neutral-100 font-mono flex items-center justify-center p-8">
      <div className="max-w-xl">
        <h1 className="text-2xl tracking-tight mb-2">PIT WALL MULTIVERSE</h1>
        <p className="text-neutral-400">
          A counterfactual Formula 1 race simulator. Scaffold is up; the pit wall
          comes online in Phase 6.
        </p>
      </div>
    </main>
  );
}
